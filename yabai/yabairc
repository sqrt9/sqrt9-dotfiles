#
#
#	by theo
#
#


#---default layout (can be bsp, stack or float)
yabai -m config layout bsp

#---new window spawns to the right if vertical split,
#---or bottom if horizontal split
yabai -m config window_placement second_child

#---setmodifier for clicking and dragging with mouse
yabai -m config mouse_modifier alt

#---set modifier + left-click drag to move window
yabai -m config mouse_action1 move

#---set modifier + right-click drag to resize window
yabai -m config mouse_action2 resize

#---swap windows when dropped in center
yabai -m mouse_drop_action swap
yabai -m config menubar_opacity 0.4

#---focus follows mouse, mouse follows focus, automatically raise windows
yabai -m config focus_follows_mouse off
yabai -m config mouse_follows_focus on

#---padding between windows
yabai -m config top_padding    3
yabai -m config bottom_padding 3
yabai -m config left_padding   3
yabai -m config right_padding  3
yabai -m config window_gap     3

#---management settings 
# yabai -m signal --add event=window_created action="yabai -m window --focus recent"

#---window border settings
yabai -m config menubar_opacity 1.0
yabai -m rule --add app="^Notes$" manage=off
yabai -m rule --add app="^Notion$" manage=off
yabai -m rule --add app="^LuLu$" manage=off
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Stickies$" manage=off
yabai -m rule --add app="^Finder$" manage=off
yabai -m rule --add app="^Find My$" manage=off
yabai -m rule --add app="^Facetime$" manage=off
yabai -m rule --add app="^osascript$" manage=off
borders active_color=0x11ffffff inactive_color=0x00000000 width=6.0 style=round ax_focus=on whitelist="Google Chrome,Finder,app_mode_loader,Messages,Facetime,Photos,Music,Spotify,Code,iTerm2,Books,Discord,Notes,Activity Monitor,Podcasts,Find My,Preview,Electron" &

# yabai     -- blacklist
# borders   -- whitelist